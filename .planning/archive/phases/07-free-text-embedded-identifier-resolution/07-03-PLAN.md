---
phase: 07-free-text-embedded-identifier-resolution
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - docs/free-text-samples.md
  - docs/free-text-unsupported-samples.md
autonomous: true
requirements:
  - EMBED-FIXTURES  # fixture docs reflect new embedded-identifier support
user_setup: []

must_haves:
  truths:
    - "The canonical embedded-DOI failing case from CONTEXT.md now appears as a supported sample in docs/free-text-samples.md"
    - "An embedded-PMID supported sample appears in docs/free-text-samples.md"
    - "docs/free-text-unsupported-samples.md no longer implies that a citation carrying an inline DOI/PMID is unsupported"
  artifacts:
    - path: "docs/free-text-samples.md"
      provides: "Embedded-identifier supported samples section"
      contains: "10.1234/abcd"
    - path: "docs/free-text-unsupported-samples.md"
      provides: "Note clarifying inline DOI/PMID citations now resolve"
      contains: "embedded"
  key_links: []
---

<objective>
Update the fixture documentation so the canonical embedded-identifier cases are
documented as supported, and the unsupported-samples doc no longer implies that a
citation carrying an inline DOI/PMID fails.

Purpose: The fixture docs are the human-facing record of what the parser supports.
After Plans 01-02, a full citation carrying an inline DOI/PMID resolves; the docs
must reflect that so reviewers and future contributors do not re-classify these as
unsupported.

Output: A new supported-samples section plus an unsupported-doc clarification note.
This is documentation only — no code, no tests.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-free-text-embedded-identifier-resolution/07-CONTEXT.md
@.planning/phases/07-free-text-embedded-identifier-resolution/07-VALIDATION.md
@docs/free-text-samples.md
@docs/free-text-unsupported-samples.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document embedded-identifier supported samples and migrate unsupported note</name>
  <files>docs/free-text-samples.md, docs/free-text-unsupported-samples.md</files>
  <action>
    In docs/free-text-samples.md, add a new top-level section
    "## Supported embedded-identifier samples" with two entries, matching the existing
    sample formatting (text fenced block + an "Expected:" bullet list):

      1. Embedded DOI (the canonical CONTEXT.md case):
         ```text
         Author. Title. Place: Publisher, 2020. https://doi.org/10.1234/abcd
         ```
         Expected:
         - resolves via the existing CrossRef DOI resolver (not the heuristic parser)
         - inputFormat: doi
         - CSL sourced from CrossRef; deduped against existing DOIs

      2. Embedded PMID:
         ```text
         Author. Title. Journal. 2019. PMID: 12345678
         ```
         Expected:
         - resolves via the existing PMID REST proxy
         - inputFormat: pmid
         - CSL sourced from NCBI

    Note in the section intro that the heuristic parser is bypassed when a citation
    carries an inline DOI (`10.\d{4,}/…`) or labeled PMID, and that resolver failure
    degrades gracefully to the heuristic parser and then the limited-support message.

    In docs/free-text-unsupported-samples.md, the four existing unsupported samples
    do NOT carry inline DOIs/PMIDs, so none of THEM migrate. Instead, under
    "## Expected behavior" (or a short new note near the top), add a clarifying line:
    "Note: a citation that carries an inline DOI (10.NNNN/...) or a labeled PMID is no
    longer unsupported — as of Phase 7 it is extracted and resolved via the existing
    DOI/PMID resolver. Samples remain here only because they lack any embedded
    identifier." Keep all four existing samples (they still lack identifiers).

    Do not reword the existing samples beyond this clarification. Do not add code.
  </action>
  <verify>
    <automated>grep -q "10.1234/abcd" docs/free-text-samples.md && grep -qi "PMID: 12345678" docs/free-text-samples.md && grep -qi "embedded" docs/free-text-unsupported-samples.md && echo OK</automated>
  </verify>
  <done>
    free-text-samples.md has an embedded-identifier section with the canonical DOI case
    and a PMID case; free-text-unsupported-samples.md carries the clarifying note.
    ONE commit: docs(free-text): document embedded-identifier resolution samples.
  </done>
</task>

</tasks>

<verification>
- `grep "10.1234/abcd" docs/free-text-samples.md` matches.
- The unsupported doc clarifies that inline-DOI/PMID citations now resolve.
- No code or test files touched in this plan.
</verification>

<success_criteria>
- Canonical embedded-DOI and an embedded-PMID sample documented as supported.
- Unsupported doc no longer implies inline-identifier citations fail.
</success_criteria>

<output>
After completion, create
`.planning/phases/07-free-text-embedded-identifier-resolution/07-03-SUMMARY.md`.
</output>
