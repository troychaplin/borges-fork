---
phase: 04-frontend-cite-export-affordances
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/style.scss
  - src/edit.js
autonomous: false
requirements:
  - CITE-EXPORT-CSS
  - CITE-EXPORT-EDITOR-TOGGLE
  - CITE-EXPORT-ACCESSIBILITY
must_haves:
  truths:
    - "The outputCiteExport toggle appears in the block inspector (Block tab, alongside outputCoins and outputCslJson)"
    - "Toggling outputCiteExport to true in the editor causes the Cite/Export panels to appear in the saved output"
    - "CSS resets text-indent to 0 inside .bibliography-builder-cite-export so the panel is not offset by the hanging-indent rule"
    - "CSS makes <summary> readable: cursor pointer, font-style normal, no inherited italic"
    - "Export links render as an inline flex list, not as indented bullet points"
  artifacts:
    - path: "src/style.scss"
      provides: "CSS for .bibliography-builder-cite-export and child selectors"
      contains: "text-indent: 0"
    - path: "src/edit.js"
      provides: "ToggleControl for outputCiteExport in InspectorControls"
      contains: "outputCiteExport"
  key_links:
    - from: "src/edit.js InspectorControls"
      to: "block attribute outputCiteExport"
      via: "setAttributes({ outputCiteExport: value })"
      pattern: "outputCiteExport"
    - from: "src/style.scss"
      to: ".bibliography-builder-cite-export"
      via: "text-indent: 0; padding-left: 0 reset"
      pattern: "text-indent"
---

<objective>
Wire the `outputCiteExport` block attribute to the editor Inspector Controls (so authors can enable the feature per block), and add CSS rules to the frontend stylesheet that prevent the hanging-indent `text-indent: -2em` from bleeding into the `<details>` disclosure panels.

Purpose: Plans 02 and 03 delivered the save markup and editor pre-computation. This plan makes the feature discoverable to authors and visually correct on the frontend.

Output: A new ToggleControl in the block inspector, and CSS reset rules in style.scss. Ends with a human-verify checkpoint to confirm visual correctness in the browser.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend-cite-export-affordances/04-CONTEXT.md
@.planning/phases/04-frontend-cite-export-affordances/04-RESEARCH.md
@.planning/phases/04-frontend-cite-export-affordances/04-02-SUMMARY.md
@.planning/phases/04-frontend-cite-export-affordances/04-03-SUMMARY.md
@src/edit.js
@src/style.scss

<interfaces>
<!-- Existing inspector controls pattern in edit.js (around line 639) -->
<PanelBody title={__('Metadata output', 'borges-bibliography-builder')}>
  <ToggleControl
    label={__('JSON-LD (structured data)', 'borges-bibliography-builder')}
    checked={outputJsonLd}
    onChange={(value) => setAttributes({ outputJsonLd: value })}
  />
  <ToggleControl
    label={__('COinS (scholarly metadata)', 'borges-bibliography-builder')}
    checked={outputCoins}
    onChange={(value) => setAttributes({ outputCoins: value })}
  />
  <ToggleControl
    label={__('CSL-JSON (machine-readable)', 'borges-bibliography-builder')}
    checked={outputCslJson}
    onChange={(value) => setAttributes({ outputCslJson: value })}
  />
  {/* Add after outputCslJson toggle: */}
  <ToggleControl
    label={__('Cite / Export affordances', 'borges-bibliography-builder')}
    help={__('Add per-entry disclosure panels with copy and download links for BibTeX, RIS, and CSL-JSON.', 'borges-bibliography-builder')}
    checked={outputCiteExport}
    onChange={(value) => setAttributes({ outputCiteExport: value })}
  />
</PanelBody>

<!-- CSS from RESEARCH.md Pattern 4 (confirmed correct) -->
.wp-block-bibliography-builder-bibliography details.bibliography-builder-cite-export {
  text-indent: 0;
  padding-left: 0;
  margin-top: 0.4em;
  font-size: 0.875em;
}

.wp-block-bibliography-builder-bibliography details.bibliography-builder-cite-export summary {
  cursor: pointer;
  font-style: normal;
  font-weight: normal;
  display: inline-block;
  color: currentColor;
}

.wp-block-bibliography-builder-bibliography details.bibliography-builder-cite-export .bibliography-builder-cite-text {
  font-style: normal;
  white-space: pre-wrap;
  margin: 0.4em 0;
}

.wp-block-bibliography-builder-bibliography details.bibliography-builder-cite-export .bibliography-builder-export-links {
  list-style: none;
  padding: 0;
  margin: 0.4em 0 0;
  display: flex;
  flex-wrap: wrap;
  gap: 0.5em;
}

<!-- In edit.js, add outputCiteExport to the destructured attributes (around line 87) -->
const {
  citationStyle,
  headingText,
  outputJsonLd = true,
  outputCoins = false,
  outputCslJson = false,
  outputCiteExport = false,  // NEW
  citations = [],
} = attributes;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS reset and disclosure panel styles to style.scss</name>
  <files>src/style.scss</files>
  <action>
    Append CSS rules for the cite/export disclosure panels to src/style.scss. Place the new rules after the existing `li` hanging-indent rules so the specificity cascade is clear.

    Add these rules verbatim (adapting SCSS nesting as appropriate for the existing file's nesting style):

    ```scss
    // Cite / Export disclosure panels — reset hanging-indent inheritance
    .wp-block-bibliography-builder-bibliography {
      details.bibliography-builder-cite-export {
        text-indent: 0;
        padding-left: 0;
        margin-top: 0.4em;
        font-size: 0.875em;

        summary {
          cursor: pointer;
          font-style: normal;
          font-weight: normal;
          display: inline-block;
          color: currentColor;
        }

        .bibliography-builder-cite-text {
          font-style: normal;
          white-space: pre-wrap;
          margin: 0.4em 0;
        }

        .bibliography-builder-export-links {
          list-style: none;
          padding: 0;
          margin: 0.4em 0 0;
          display: flex;
          flex-wrap: wrap;
          gap: 0.5em;
        }
      }
    }
    ```

    Match the SCSS nesting style already in use in the file (check if the existing rules use BEM-flat or nested selectors). The `text-indent: 0` and `padding-left: 0` resets are the critical rules — do not omit them.

    After adding, run: npm run lint:css to confirm no SCSS errors.
  </action>
  <verify>
    <automated>npm run lint:css</automated>
  </verify>
  <done>style.scss passes CSS lint with no errors. The file contains text-indent: 0 inside .bibliography-builder-cite-export.</done>
</task>

<task type="auto">
  <name>Task 2: Add outputCiteExport toggle to block inspector in edit.js</name>
  <files>src/edit.js</files>
  <action>
    Two changes to edit.js:

    1. Add `outputCiteExport = false` to the attributes destructuring at the top of the edit component function (same location as outputJsonLd, outputCoins, outputCslJson — around line 87).

    2. Add a ToggleControl for outputCiteExport at the end of the existing "Metadata output" PanelBody, after the existing outputCslJson ToggleControl:
       ```jsx
       <ToggleControl
         label={__('Cite / Export affordances', 'borges-bibliography-builder')}
         help={__(
           'Add per-entry disclosure panels with copy and download links for BibTeX, RIS, and CSL-JSON.',
           'borges-bibliography-builder'
         )}
         checked={outputCiteExport}
         onChange={(value) => setAttributes({ outputCiteExport: value })}
       />
       ```

    Do NOT add a new PanelBody — place the toggle inside the existing "Metadata output" panel alongside the other output-control toggles.

    After adding, run: npm run lint:js to confirm no JS errors.
  </action>
  <verify>
    <automated>npm run lint:js</automated>
  </verify>
  <done>edit.js passes JS lint. outputCiteExport is destructured from attributes and a ToggleControl for it exists in the InspectorControls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All four plans of Phase 4 are now complete:
    - Plan 01: outputCiteExport attribute, cslToRisEntry export, deprecated entry
    - Plan 02: <details> panels in save() with RIS and CSL-JSON data URIs, BibTeX/BibLaTeX conditional on pre-stored strings
    - Plan 03: Editor hooks pre-compute and store exportBibtex/exportBiblatex per citation
    - Plan 04: CSS reset rules, outputCiteExport toggle in inspector

    Run `npm run build` first to produce updated build assets.
  </what-built>
  <how-to-verify>
    1. Run `npm run build` — confirm build succeeds with no errors.
    2. Run `npm test` — confirm all tests pass.
    3. Open WordPress with the plugin active. Add or open a post with a Bibliography block that has at least one citation.
    4. In the Block Inspector (right sidebar), find the "Metadata output" panel. Confirm a new "Cite / Export affordances" toggle is present.
    5. Toggle it ON. Save/update the post.
    6. View the post on the frontend. Confirm:
       a. Each bibliography entry shows a "Cite / Export" disclosure widget below it.
       b. Click to open one panel. Confirm the citation text is readable inside the panel.
       c. Confirm RIS and CSL-JSON download links are present.
       d. If the citations have BibTeX/BibLaTeX pre-computed (added after this feature is enabled), confirm those links also appear.
       e. Confirm the panel text is flush-left, not offset by a hanging indent.
       f. Confirm the export links appear as an inline row, not a bulleted list.
    7. Toggle the feature OFF. Save/update. Confirm the panels disappear from the frontend output.
    8. Deactivate the plugin. Confirm the static bibliography text remains readable (the <details> panels remain in markup but are collapsed by default and the citation text is still visible).
  </how-to-verify>
  <resume-signal>Type "approved" if the feature looks and works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
npm run build — build succeeds
npm test — full suite green
Human verification of visual CSS correctness and feature discoverability in the editor
</verification>

<success_criteria>
- style.scss contains text-indent: 0 reset inside .bibliography-builder-cite-export
- npm run lint:css passes
- edit.js has outputCiteExport destructured and a ToggleControl in the inspector
- npm run lint:js passes
- npm run build succeeds
- npm test (full suite) is green
- Human approval of visual correctness in the browser
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-cite-export-affordances/04-04-SUMMARY.md`
</output>
